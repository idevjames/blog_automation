# settings.py
# =========================================================
# [사용자 설정 파일] 
# 이 파일의 내용을 수정하면 프로그램 동작이 바뀝니다.
# =========================================================

# 1. 서로이웃 탐색 주제 (번호 입력)
# [주요 코드 안내]
# 9: 드라마
# 10: 방송
# 12: 스타/연예인
TARGET_DIRECTORY = 10

# 2. 서로이웃 신청 메시지 목록
# 이 중에서 랜덤으로 하나가 선택되어 발송됩니다.
NEIGHBOR_MESSAGES = [
    "안녕하세요! 관심 분ㅑㅏ 비슷해 서로이웃 신청합니다. 잘 부탁드려요 :)",
    "반갑습니다~ 블로그 글 잘 보고 가요! 소통하며 지내고 싶어 서로이웃 신청해요.",
    "안녕하세요, 관심 분야가 같아서 이웃 신청합니다. 자주 소통해요!",
    "좋은 글이 많아서 구독하고 싶어요. 서로이웃 받아주시면 감사하겠습니다 ^^"
]

# 3. 행동별 랜덤 대기 시간 설정 (단위: 초)
# (최소시간, 최대시간) 사이에서 랜덤하게 쉽니다.
# 너무 빠르게 설정하면 네이버 봇 탐지에 걸릴 수 있습니다.
CUSTOM_DELAYS = {
    # 페이지 로딩 후 대기 (인터넷이 느리면 값을 늘리세요)
    "page_load": (2.5, 4.5),       
    
    # 버튼 발견 후 클릭 전 잠깐 망설임 (사람처럼 보이기 위함)
    "before_click": (0.5, 0.5),    
    
    # 동작 하나 끝내고 다음 동작까지 휴식
    "between_actions": (1.0, 2.9), 
    
    # 페이지 번호 누르고 이동 대기
    "page_nav": (2.0, 4.0),        
    
    # 공감/이웃 버튼이 제대로 눌렸는지 확인하는 간격
    "verify_interval": (1.0, 1.0), 
    
    # 실패 시 재시도 전 대기
    "retry_wait": (1.5, 1.5),      
    
    # [중요] 마우스 클릭 오차 범위 (숫자가 클수록 더 중앙에 가깝게 클릭)
    # 3 = 버튼 크기의 1/3 범위 내에서 랜덤 클릭
    "click_offset_ratio": 3,
    
    # ----------------------------------------
    # [서로이웃 팝업 관련 시간]
    # ----------------------------------------
    "window_switch": (1.5, 2.5),   # 새 창이 열릴 때까지 대기
    "popup_step_wait": (0.8, 1.2), # 팝업 뜨고 나서 멍때리기
    "popup_interaction": (0.5, 0.8), # 라벨/입력창 클릭 후 대기
    "popup_form_load": (1.5, 2.0),   # '다음' 버튼 누른 후 화면전환 대기
    "popup_typing": (0.5, 1.0),      # 메시지 입력 후 대기
    "popup_submit": (2.0, 3.0)       # 최종 제출 후 완료 대기
}

# 4. 기본 설정값
DEFAULT_LIKE_COUNT = 10       # 공감 개수 엔터치면 적용되는 기본값
DEFAULT_FAILURE_COUNT = 3     # 공감 연속 실패 시 중단 기준